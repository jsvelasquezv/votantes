<div class="panel panel-default">
    <div class="panel-heading">
        <div class="panel-title">
            <h1>
                <%= button_to 'Atras', votantes_path, class: 'btn btn-primary pull-right', method: :get %>
                Editando 
                <%= @votante.nombres %> 
                <%= @votante.apellidos %>
            </h1>
        </div>
    </div>
    <div class="panel-body">
        <%= render 'form' %>
    </div>
</div>
